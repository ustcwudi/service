spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://root:root@192.168.3.11:27017/assist}
  redis:
    host: ${REDIS_HOST:192.168.3.11}
    port: ${REDIS_PORT:6379}
    password: ${REDIS_PASSWORD:123456}
    database: 0
    timeout: 5000
minio:
  endpoint: ${MINIO_ENDPOINT:http://192.168.3.11:9000}
  accessKey: ${MINIO_ACCESS_KEY:root}
  secretKey: ${MINIO_SECRET_KEY:12345678}
  bucket: ${MINIO_BUCKET:assist}
service:
  secret: ${CUSTOM_SECRET:hubu!@#$%^&^}
wechat:
  id: ${WECHAT_ID:unknow}
  secret: ${WECHAT_SECRET:unknow}
